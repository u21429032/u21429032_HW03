@model IEnumerable<ismail_HomeworkAssignment3.Models.FileModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="row">
    @foreach (var item in Model)
    {
    <div class="col-md-3 gallery">
        <a href="~/Media/Videos/@item.FileName" data-fancybox="gallery" data-type="video" data-caption="@item.FileName">
            <video controls width="300" height="300">
                <source src="~/Media/Videos/@item.FileName" type="video/mp4">
            </video>
        </a>

        <p> @item.FileName</p>
        <button type="button" class="btn btn-success">
            @Html.ActionLink("Download", "Download", new { name = item.FileName }, new { @Style = "color: white;" })
        </button>
        <button type="button" class="btn btn-danger">
            @Html.ActionLink("Delete", "Delete", new { name = item.FileName }, new { @Style = "color: white;" })
        </button>
    </div>
    }
</div>


<script>
        $(document).ready(function () {
            $(".gallery a").fancybox();
        });
</script>